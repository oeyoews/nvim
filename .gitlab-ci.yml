image: node:latest

stages:
  - release
  - deploy

create-release:
  stage: release
  except:
    changes:
      - package.json
      - CHANGELOG.md

  variables:
    GITLAB_TOKEN: $GITLAB_TOKEN

  script:
    - yarn install
    - npx semantic-release
  rules:
    - if: $CI_COMMIT_TAG
